package com.nexushr.auth.dashboard.repository;

import org.springframework.stereotype.Repository;

@Repository
public class DashboardRepository {

    /*
     * Dashboard data will be collected from
     * Employee, Department, Attendance, Leave,
     * Payroll and other modules.
     *
     * This class can later contain custom
     * database queries.
     */

}